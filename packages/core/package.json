{
  "name": "@de-re-crud/core",
  "description": "Core library for creating De Re CRUD interfaces and services",
  "version": "0.1.4",
  "repository": "https://github.com/DeReCRUD/de-re-crud.git",
  "license": "Apache-2.0",
  "main": "dist/de-re-crud-core.umd.js",
  "module": "dist/de-re-crud-core.es5.js",
  "types": "dist/types",
  "scripts": {
    "lint": "tslint --project ./tsconfig.json",
    "clean": "del-cli \"dist/**/*\"",
    "build:dev": "rollup --config",
    "build:prod": "rollup --config",
    "build": "npm-run-all -s clean build:dev build:prod"
  },
  "dependencies": {
    "preact": "~8"
  },
  "devDependencies": {
    "redux-zero": "^4.12.0"
  }
}
